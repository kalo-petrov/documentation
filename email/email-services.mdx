---
title: Email Services
description: Configure how AffZero sends emails — via Gmail SMTP using an App Password.
---

## Overview

AffZero sends outbound emails using **Gmail SMTP** — Google's mail-sending service authenticated with an App Password. This works with both personal Gmail (`@gmail.com`) and Google Workspace accounts (`@yourcompany.com`).

Go to **Providers → Email** to add or manage your email accounts.

<Note>
  If you also want to save files to Google Drive, use Google Doc invoice templates, or write to Google Sheets, connect your Google account via the **Gmail & GDrive** integration instead. See [Gmail & Google Drive](/providers/gmail-drive) for details.
</Note>

---

## Setting Up Gmail SMTP

Gmail requires an **App Password** — a 16-character code that lets AffZero send email on your behalf without using your main Google account password.

### Step 1 — Enable 2-Step Verification

App Passwords are only available when 2-Step Verification is turned on.

1. Go to your [Google Account Security settings](https://myaccount.google.com/security)
2. Under **"How you sign in to Google"**, click **2-Step Verification**
3. Follow the prompts to enable it if it isn't already on

<Note>
  **Google Workspace users:** your admin may have already enforced 2-Step Verification. If the App Passwords option is missing, ask your admin to enable it for your account.
</Note>

### Step 2 — Create an App Password

1. Go to [myaccount.google.com/apppasswords](https://myaccount.google.com/apppasswords)
   *(You must be signed in to the correct Google account)*
2. In the **"App name"** field, type something like `AffZero` so you can identify it later
3. Click **Create**
4. Google shows a **16-character password** (4 groups of 4 letters). **Copy it now** — Google won't show it again
5. Remove the spaces before pasting it into AffZero

<Tip>
  You can always revoke and regenerate the App Password later from the same page without affecting your main Google account password.
</Tip>

### Step 3 — Add the account in AffZero

1. In AffZero, go to **Providers → Email**
2. Click **Add Gmail Account**
3. Fill in the form:

| Field | What to enter |
|-------|--------------|
| **Gmail Address** | Your full Gmail or Workspace address (e.g. `you@gmail.com` or `you@company.com`) |
| **App Password** | The 16-character code from Step 2 (no spaces) |
| **Sender Name** | The display name recipients will see (e.g. `Your Name` or `Your Company`) |

4. Click **Add Gmail Account** — AffZero encrypts the App Password (AES-256-GCM) and saves the account

Your Gmail address is now available as a **Send From** option in every Send Email step.

---

## Multiple Accounts

You can connect multiple Gmail accounts and choose which one to send from in each automation. This is useful when you want to send affiliate emails from one address and billing emails from another.

| Plan | Gmail accounts |
|------|---------------|
| Starter | 1 |
| Pro | 2 |
| Pro+ | 3 |
| Business | 3 |

If you have multiple accounts, one is marked **Primary** — this is the default when an automation doesn't specify a sender. Click **Set Primary** next to any account to change it.

---

## Outlook, Yahoo, and other providers

AffZero's current SMTP integration is built specifically for Gmail and Google Workspace App Passwords. Outlook, Yahoo, and other SMTP providers are not supported at this time.

If you need to send from a custom domain address, the recommended path is to set up a free **Google Workspace** account for that domain and connect it here using the same App Password flow.

---

## Email Forwarding (Receiving Emails)

In addition to sending emails, AffZero can **receive** emails via forwarding addresses. This is a separate feature used by the **Email Received** trigger — not for sending.

Forwarding addresses look like `auto-7gk2p9xr@inbox.affzero.com`. When you forward an email there (or set up automatic forwarding from your inbox), AffZero runs the automation linked to that address.

**To set up forwarding:**
1. Go to **Providers → Email Forwarding**
2. Create a forwarding address and give it a label (e.g. "Vendor invoices")
3. In your email client, forward messages to that address — manually or via an automatic rule

See [Email Received Trigger](/automations/triggers/inbound-email) for full setup instructions including Gmail and Outlook forwarding rules.

---

## Troubleshooting

<AccordionGroup>
  <Accordion title="'Authentication failed' when adding the account">
    The App Password was typed or pasted incorrectly. Make sure you:
    - Removed all spaces from the 16-character code
    - Used the correct Gmail address (the one the App Password belongs to)
    - Have 2-Step Verification enabled on that account

    If in doubt, go back to [myaccount.google.com/apppasswords](https://myaccount.google.com/apppasswords), delete the old password, and generate a new one.
  </Accordion>

  <Accordion title="Email not delivered">
    Check that your Google account hasn't revoked AffZero's app access. Go to **Google Account → Security → How you sign in → App Passwords** and verify the AffZero entry is still listed. If it's missing, generate a new one and update it in AffZero.

    Also check Gmail's daily sending limits: personal Gmail accounts allow roughly **500 emails/day**; Google Workspace accounts allow up to **2,000/day**.
  </Accordion>

  <Accordion title="I don't see the App Passwords option in my Google account">
    App Passwords only appear when **2-Step Verification is enabled**. If the option is missing even after enabling 2-Step Verification, your Google Workspace admin may have restricted App Passwords. Contact your admin and ask them to allow App Passwords for your account.
  </Accordion>

  <Accordion title="Emails landing in spam">
    Gmail SMTP sends through Google's own infrastructure, so spam rates are generally low. If emails are landing in spam:
    - Ask recipients to mark your emails as "Not spam" and add your address to contacts
    - Avoid spam trigger words in subject lines
    - Make sure the sender name is recognisable to recipients
  </Accordion>

  <Accordion title="I changed my Google password — does AffZero stop working?">
    No — App Passwords are independent of your main Google password. Changing your password does not invalidate App Passwords. However, if you **revoke** the App Password from Google's security settings, you'll need to generate a new one and re-add the account in AffZero.
  </Accordion>
</AccordionGroup>
